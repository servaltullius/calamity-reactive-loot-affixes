# Changelog

이 프로젝트의 주요 변경사항은 이 파일에 기록합니다.

형식은 [Keep a Changelog](https://keepachangelog.com/en/1.1.0/)를 따르고,
버전 표기는 [Semantic Versioning](https://semver.org/spec/v2.0.0.html) 기반으로 관리합니다.

## [Unreleased]

### Changed

- 확인 스크롤로 일반 어픽스가 없는 장비에 접두 1개 및 접미 0~2개를 부여합니다(1/2/3개: 60%/30%/10%).
- 재련은 선택한 일반 어픽스 하나를 교체하며 재련 오브 2개를 사용합니다. 슬롯 확장 비용 2/4개와 횟수 무제한을 유지합니다.
- 정제 오브 1개로 슬롯 수를 유지한 채 일반 어픽스 전부를 한 번에 다시 굴립니다. 룬워드와 그 성장 상태는 유지하고, 기존 일반 어픽스의 성장 상태는 초기화됩니다. 구버전·비정상 어픽스 구성도 정제로 정상 구성(접두 1 + 접미)으로 되돌릴 수 있습니다.
- 적격 적대 시체에 확인 스크롤 20% / 정제 오브 2% 독립 드랍을 추가합니다. 기존 재화 보상은 유지합니다.
- 패널에 재화 보유량, 작업 가능 조건, 정제 재확인, 연속 클릭 방지를 추가합니다. 무료 전체 초기화는 디버그 모드에서만 가능합니다.
- 새 재화 레코드 2개를 ESP 끝에 추가해 기존 FormID를 보존합니다. 인게임 검증 전 테스트 변경입니다.
- MCM에 확인 스크롤·정제 오브 드랍 확률 슬라이더(기본 20% / 2%)를 추가하고, 루팅 정책·안내 문구를 새 제작 흐름에 맞게 고쳤습니다.
- 패널의 "재련 자원"을 "제작 재료"로 바꿔 재련 오브·확인 스크롤·정제 오브 보유량을 한곳에 표시합니다.
- 제작 버튼을 흐름 순서(확인 → 선택 재련 → 슬롯 확장 → 정제)로 배치하고, 교체할 어픽스 목록을 기본으로 펼칩니다.
- 디버그 모드가 아니면 쓸 수 없는 복구·초기화 섹션을 숨기고, 초기화 버튼의 비활성 사유 안내를 바로잡았습니다.
- 한국어 모드에서 "가리킨 아이템 확인" 제목이 두 언어로 표시되던 문제를 수정했습니다.
- 디버그 스타터 지급이 재련 오브 3개에 더해 확인 스크롤 3개·정제 오브 1개를 함께 지급합니다.
- 넓은 패널 레이아웃(기본 1320×860 포함)의 룬워드 탭에서 레시피 목록이 한 항목도 다 보이지 않을 만큼(약 30px) 줄어들던 문제를 수정했습니다. UI 배율이 패널 크기에 비례해 커지므로 패널을 키워도 해결되지 않았습니다(정식 1.7.5에도 존재). 이제 작업대가 공간이 충분하면 고정되고, 부족하면 최소 높이를 보장한 채 작업 영역 전체가 스크롤됩니다.
- 룬 그리드 칸이 38px로 고정돼 베이스 이름과 룬 정보가 글자 단위로 끊기고 잘리던 문제를 수정했습니다. 칸을 56px로 키우고 긴 이름은 두 줄 말줄임과 마우스 오버 전체 표시로 처리합니다. 룬 그리드를 펼쳤을 때 검토 카드 아래쪽이 잘리던 문제도 스크롤로 바꿨습니다.
- 작은 패널에서 "베이스 변경" 목록이 패널 아래로 넘쳐 마지막 장비를 고를 수 없던 문제를 수정했습니다. 목록이 들어가면 그대로 보여주고, 넘칠 때만 남은 공간에 맞춰 스크롤합니다.

## [1.7.5] - 2026-09-22

v1.7.5 정식 릴리스는 RC6의 전투 프리징 수정·일반 공격 발동·중복 옵션 강화에 Calamity 자동 소환수의 아군 피해 보호를 추가합니다. 2026-09-22 사용자가 시작 CTD 수정 후 소환수 보호가 정상으로 보인다고 확인했습니다.

### Added

- **치명타·강공격 전용 효과 8종에 약 2.5타당 1회를 목표로 한 일반 무기 공격 발동 경로를 추가했습니다.** 곰 덫은 일반 무기 적중 시 40%, 파이어볼트·아이스 스파이크·라이트닝 볼트는 일반 근접 적중 시 45%, 썬더볼트·아이스 스피어·체인 라이트닝·아이스 스톰은 35%로 판정합니다. 치명 시전 7종을 모두 후보로 두면 평균 39.3%로 약 2.55타당 한 번 발동합니다. 치명타·강공격은 기존 확률만 사용하고 평타 확률을 이중 판정하지 않으며, 활·석궁 치명 시전은 기존처럼 모든 정상 무기 적중에서 100% 경로를 유지합니다. 주문·배시·타임드 배시·폭발은 평타 경로에서 제외하고 모든 경로가 기존 ICD를 공유합니다.
- **서로 다른 장비에 붙은 동일 표준 발동 어픽스와 단계형 접미가 중복 장착으로 강화됩니다.** 표준 발동 어픽스는 각 아이템의 슬롯 페널티가 반영된 성공 확률을 강한 사본부터 최대 3개까지 점감 합성하되 효과·발동 예산·ICD는 한 번만 소비합니다. 단계형 접미는 장착 티어 점수를 합산해 `T1+T1→T2`, `T1+T2→T3`으로 승급하며 T3에서 제한됩니다. 한 아이템 내 동일 토큰 금지, 특수 발동의 공유 판정, Scroll Mastery의 기존 합산·100% 상한은 유지합니다.

### Changed

- **근접 치명타·강공 한 번에 서로 다른 치명 시전 주문을 최대 2개까지 공정하게 발동할 수 있습니다.** 일반 근접 공격과 활·석궁 적중은 최대 1개를 유지하고, 0.15초 전역 ICD는 공격별 발동 묶음에 한 번 적용하며 선택된 어픽스만 개별·대상별 ICD를 소비합니다. 수동 VFX·SFX도 묶음당 한 번만 재생합니다.

### Fixed

- **Calamity 장비 효과로 자동 소환된 패밀리어·정령을 플레이어와 아군의 공격으로부터 보호합니다.** 정확히 식별한 소환 개체에 아군 피격 무시를 적용하고, 중첩된 체력 피해와 유해 마법·인챈트 적용을 차단합니다. 적이 소환수에게 주는 피해와 일반 수동 소환수의 기존 규칙은 유지하도록 구성했습니다.
- **소환수 보호 정보를 저장하고 로드 후 복원합니다.** 별도 CSUM 레코드에 실제 소환 개체 ID만 저장하며, 예전 세이브에서 이미 살아 있던 소환수는 식별 기록이 없으므로 새로 소환해야 합니다.
- **소환수 보호 테스트 중 발생한 시작 CTD를 수정했습니다.** 명시적인 Actor/Character/PlayerCharacter 함수 테이블을 사용하고, 배열 크기를 컴파일 단계에서 검사합니다. 보호 등록·피격·피해 차단은 제한된 횟수로 로그에 남깁니다.
- **전투 중 마법효과 이벤트와 Calamity 상태 락이 서로를 기다리던 교착 경로를 수정했습니다.** 8개 이벤트 진입점은 상태 락을 즉시 얻지 못하면 소유권 있는 이벤트 스냅샷을 순서대로 지연 처리합니다. 피격 데이터와 원래 재진입 문맥을 보존하고 로드·Revert 이후 이전 작업을 무효화합니다. 기존 발동·ICD·아군 보호 상태 잠금은 유지합니다. 두 덤프에서 같은 순환을 재확인했고, 동시성 회귀 검사와 테스트 패키지 검증을 통과했으며 2026-09-05 사용자가 프리징이 해결된 것으로 보인다고 보고했습니다.
- **Calamity 공격 효과가 시전자·아군·중립 NPC에게 피해를 주는 경로를 차단했습니다.** 대상형 주문·적응 원소·피해 전환·치명 시전·아크메이지·함정·시체 폭발은 현재 적대 관계인 대상에게만 실행합니다. 생성된 공격성 마법효과에는 `PlayerRef` 적대 및 팀메이트 제외 조건을 넣고, 치명 시전 7종과 곰덫의 외부 바닐라 주문 2종은 같은 조건을 모든 효과에 적용한 전용 복제본으로 교체해 광역 피해·감속·충격·추방·퇴치의 주변 오발을 막습니다. 폭발 패밀리어처럼 소환체를 거치는 체력 피해는 Calamity 시전으로 실제 생성된 소환체 핸들과 그 소환체가 소유·유발한 폭발만 추적해 비적대 대상 피해를 차단합니다. 등록된 Calamity 소환수를 제외한 대상에는 일반 플레이어 공격과 수동 주문의 기존 아군 피해 규칙을 유지합니다.

### Earlier release candidates (rc1–rc5)

v1.7.5는 함정 6종의 지속 마커를 바닐라 물리 모델로 전환하고, 곰덫의 실제 열림·발동·재장전 애니메이션을 복원한 함정 시인성 릴리스입니다. 곰덫 외 범위 함정은 엔진 광역 적용 대신 런타임이 적대 대상만 골라 최대 2명에게 효과를 적용하며, 시체 폭발 대상 수명과 코세이브 writer·장착 효과 재계산 회귀 안전망도 보강했습니다. rc1은 바닐라 모델 6종의 지면 표시와 곰덫 애니메이션을 인게임에서 확인한 첫 공개 테스트 빌드입니다. rc2는 진단 전용 파티클을 실제 함정과 같은 월드 레퍼런스 생성·애니메이션·정리 경로로 교체하고, 6종 표시와 자동 정리를 인게임에서 다시 확인한 QA·릴리스 강화 빌드입니다. rc3는 룬워드 제작·재련의 자원 안전성과 목표화, 활·석궁 및 소환물 적중 판정, 장착 빌드와 룬 재료 가시성을 보강했습니다. rc4는 Prisma 작업 화면을 어픽스 중심으로 재구성하고, 룬·재련 자원과 피티를 한눈에 확인하는 대시보드 및 스크롤 없는 베이스 선택창을 추가했습니다. rc5는 일반 어픽스 슬롯을 `1→2→3`으로 명시적으로 성장시키는 정식 진행 경로와 드래곤의 룬 조각·재련 오브 확정 보상을 추가한 공개 테스트 빌드입니다.

### Fixed

- **베이스 선택창에서 다른 장비를 눌러도 선택이 바뀌지 않던 문제를 수정했습니다.** Prisma의 포커스 이동 중 선택창을 먼저 닫아 실제 클릭을 취소할 수 있던 `focusout` 자동 닫기를 제거했습니다. 바깥 클릭, `Esc`, 탭 전환, 패널 닫기와 정상 선택 후 닫기는 그대로 유지합니다.
- **이미 완성한 동일 룬워드에 조각을 다시 소비할 수 있던 경로를 차단했습니다.** 동일 결과 룬워드는 조각 계산·소비 전에 서버에서 거부하며 패널도 `완료`로 표시합니다. 다른 룬워드를 명시적으로 선택하는 기존 교체·롤백 흐름은 유지합니다.
- **활·석궁 치명 시전과 함정의 적중 출처 판정을 보강했습니다.** `HitData.weapon`이 비어 있는 실제 화살·볼트는 투사체의 활·석궁 `weaponSource` 또는 현재 원거리 공격 데이터가 확인될 때만 치명 시전을 허용합니다. 주문 적중과 단순 장착 무기 fallback은 계속 차단하며, 플레이어 소환물의 주문 적중이 플레이어 장착 무기 때문에 함정의 `weapon hit`으로 오인되던 경로도 막았습니다.
- **재련이 기존 일반 어픽스 개수를 줄일 수 있던 문제를 수정했습니다.** 2·3옵션 장비도 현재 일반 어픽스 개수를 유지한 채 같은 개수만 다시 굴리며, 일반 어픽스가 없는 베이스만 최소 1개를 얻습니다. 완성 룬워드는 기존처럼 별도로 보존됩니다.
- **바닐라 곰덫 표시물이 닫힌 기본 자세로만 생성되던 문제를 수정했습니다.** 스크립트 없는 표시용 월드 레퍼런스에 바닐라 행동 그래프 이벤트를 직접 전달해 생성 시 `StartOpen`, 최초 발동 직전 `Trigger01`, 재장전 중 `Reset01`을 재생합니다. 열림 이벤트가 수락된 뒤에는 900ms 동안 발동을 막아 애니메이션이 끝나기 전에 다시 닫히지 않게 하며, 그래프 로딩이 늦으면 250ms 간격으로 최대 8회만 재시도한 뒤 게임플레이는 fail-open으로 계속됩니다. 그래프 자체의 발동·재장전음을 단일 권위로 사용하도록 기존 명시적 중복 사운드는 제거했습니다.

- **곰덫 지면 마커를 실제 바닐라 금속 곰덫 외형으로 전환했습니다.** `BSTempEffectParticle`로 정적 곰덫 NIF를 재생하는 경로는 월드 변환을 만들지 못하므로 더 이상 사용하지 않습니다. 대신 스크립트 없는 Calamity 소유 `MSTT` 레코드가 `Must Update Anims` 플래그와 함께 바닐라 `Traps\BearTrap\BearTrap01.nif`만 참조하고, 런타임이 함정 위치에 비영속 월드 레퍼런스로 배치합니다. 원본 `BearTrap01` ACTI의 `TrapBear` 피해 스크립트는 가져오지 않으며, 생성된 표시물은 활성화·충돌을 차단하고 발동 소모·만료·cap 축출·셀 이탈·설정 초기화·저장 직전·로드/Revert에서 동일한 정리 경로로 비활성화·삭제합니다. 저장 직전에는 활성 덫 자체도 취소하고, 즉시 해석되지 않은 핸들은 48칸 고정 정리 큐에 남겨 추적과 상한을 잃지 않습니다. 기존 레코드 FormID는 그대로 두고 새 `MSTT`를 append-only 끝에 추가합니다.

- **곰덫 외 함정 5종의 지속 마커도 임시 룬 글리프에서 바닐라 물리 모델로 전환했습니다.** 룬 함정은 석재 압력판, 역병 포자는 독거미 주머니, 역청 황폐는 기름 웅덩이, 흡수 포자는 흰거미 알, 혼돈의 룬은 금속 압력판을 사용합니다. 각 모델은 원본 액티베이터나 스크립트를 소환하지 않는 Calamity 소유 `MSTT`에 경로로만 연결되며, 런타임은 이를 스크립트 없는 비영속 월드 레퍼런스로 배치하고 활성화와 충돌을 차단합니다. 기존의 짧은 무장·발동·자연 만료 파티클/음향 큐는 상태 피드백으로 유지합니다. 여섯 모델의 지면 배치와 육안 가시성은 인게임에서 확인했으며, 장시간 전투·셀 전환 뒤 잔류 없음은 계속 회귀 확인합니다.

- **함정 범위 주문이 주변 동료에게 함께 적용될 수 있던 경로를 차단했습니다.** 곰덫 외 5종의 주문 레코드는 `Effect.Area=0`으로 바꾸고, 함정 런타임이 설정 반경 안의 적대 대상만 직접 골라 발동당 최대 2명에게 시전합니다. 기존 피해·감속·저주 수치와 지속시간은 유지합니다. 한 대상에게 주 주문과 보조 주문을 함께 적용하는 함정은 틱 예산도 두 주문분을 원자적으로 예약해, 예산 경계에서 감속만 걸리고 방어 저하·흡수·저주가 빠지는 반쪽 발동을 막습니다. 동료·중립 NPC가 실제 전투에서 영향을 받지 않는지는 별도 인게임 QA 항목으로 남깁니다.

- **시체 폭발이 여러 대상에게 연쇄 시전되는 동안 앞선 주문의 엔진 재진입으로 뒤쪽 대상 참조가 무효화될 수 있던 수명 위험을 보강했습니다.** 대상 목록은 전체 시전 루프가 끝날 때까지 `NiPointer<Actor>` 강한 참조를 보유하며, 거리 정렬·대상 상한·사망 재검사·피해량은 바꾸지 않습니다.

- 함정 지면 마커(곰덫 모델 등)가 로그상 스폰 수락(`spawned=true`)에도 화면에 렌더되지 않던 파티클 경로의 실패 경계를 계측으로 확정했습니다. 마커·큐 스폰을 오일러 회전(`NiPoint3`) 오버로드에서, 실제로 렌더가 확인된 엔진 호출부들이 쓰는 항등 행렬(`NiMatrix3`) 오버로드로 전환했습니다(플래그 7 유지). 두 오버로드는 서로 다른 엔진 함수(29218/29219)입니다. 발밑 프로브(3모델 전부 수락·전부 미표시)로 이 가설이 기각된 뒤, 바닐라 메시 경로가 BSA에 실재함을 파서로 실측 확인하고, 검증된 렌더 사례(po3 Papyrus Extender)와의 마지막 차이인 **스폰 앵커 노드(`a_target`)**를 실전 마커·큐 스폰에 적용했습니다(플레이어 3인칭 3D 루트, 로그에 `anchored` 필드 추가). Spawn 반환값은 비동기 모델 클론 성공을 보증하지 않는다는 점(`BGSParticleObjectCloneTask`)도 확인했습니다.

### Added

- **일반 어픽스 슬롯을 명시적으로 1개에서 3개까지 확장할 수 있습니다.** 첫 일반 어픽스는 기존처럼 재련 오브 1개로 생성하고, 이후 슬롯 확장은 `1→2`에 오브 2개, `2→3`에 오브 4개를 사용합니다. 확장 시 기존 일반 어픽스와 완성 룬워드 및 각 런타임 상태를 유지한 채 접미 어픽스 하나만 추가하며, 세 번째 슬롯은 기존 접미와 다른 계열을 선택합니다. 아이템 어픽스 탭에는 `접두·접미 1·접미 2` 진행도와 서버가 계산한 비용·사용 가능 상태를 표시합니다. 빠른 중복 요청, 베이스·슬롯 상태 변경, 후보 없음, 정확하지 않은 오브 차감은 아이템 상태를 바꾸기 전에 거부하거나 관측된 소비량만 환불합니다. 저장 레코드 형식은 변경하지 않습니다.
- **드래곤 처치 보상을 보스 등급으로 보장합니다.** `ActorTypeDragon` 키워드를 가진 적격 적대 드래곤은 `LocRefTypeBoss` 지정 여부와 관계없이 시체에 룬 조각 1개와 재련 오브 1개를 각각 받습니다. 기존 시체별 중복 방지와 통화 보유 검사, 일반 드랍 피티 비소비 계약은 그대로 유지합니다.
- **Prisma에 룬 보유량·피티 대시보드를 추가했습니다.** 룬워드 탭에서 현재 인벤토리의 33종 룬별 수량, 보유 종류·총 조각 수와 일반 룬 조각 피티를 확인하고, 아이템 어픽스 탭에서는 재련 오브 수와 오브 피티를 확인할 수 있습니다. 진행도는 각각 `N/99`, `N/39`이며 킬 수가 아니라 확률이 0보다 큰 적격 일반 드랍 판정의 실패 횟수입니다. 보장 상태에서는 다음 적격 일반 판정을 보장하고, 보스·유니크의 별도 보장 보상은 진행도를 올리거나 초기화하지 않습니다. 화면은 기존 저장 상태와 2초 패널 갱신을 읽기만 하며 드랍 확률·피티 기준·코세이브 형식은 변경하지 않습니다.
- **재련할 때 일반 어픽스 하나를 보호할 수 있습니다.** 일반 재련은 오브 1개, 보호 재련은 오브 2개를 사용합니다. 보호한 접두 또는 접미와 현재 일반 어픽스 개수, 완성 룬워드, 보호 어픽스의 진화·모드 상태는 유지되며, 장비 전환 뒤 오래된 UI 선택은 비용과 RNG 상태를 바꾸기 전에 서버가 거부합니다.
- **Prisma `아이템 어픽스` 탭에 현재 장착 빌드 요약을 추가했습니다.** 장착 효과를 공격 발동·피격/생존·처치·패시브로 나누고, 현재 보정이 반영된 조건부 발동 판정률과 별도 Lucky Hit 관문, 장착 개수, 룬워드/접두/접미 구분을 표시합니다. 동일 발동 어픽스의 공유 판정과 단계형 접미의 최고 티어·억제 상태도 구분합니다. 이 화면은 읽기 전용입니다.
- **룬워드 레시피 탐색기에 `룬 조각 준비`·`1개 부족` 필터와 보유 진행 표시를 추가했습니다.** 같은 룬이 반복되는 요구량도 합산하고 베이스 유형·검색 필터와 함께 적용합니다. 이 표시는 전체 레시피에 필요한 현재 인벤토리 조각만 비교하며, 기존 삽입 진행·베이스 호환성·실제 변환 가능 여부를 뜻하지 않습니다.
- 함정 마커 렌더 진단용 "발밑 함정 마커 프로브" 디버그 버튼을 추가했습니다(디버그 토글이 켜진 경우에만 표시). 플레이어 발밑에 10초간 마커를 강제 스폰해, 게임플레이 변수(대상 위치·장전 타이밍·TTL)를 배제하고 "이 경로가 그려지긴 하는가"를 한 번에 판정합니다. 2차 개정에서 5변형 매트릭스(곰덫·소울트랩 각 {앵커 없음/플레이어 3D 앵커} + po3 호출 완전 복제형)로 확장했으나 전 변형이 수락-미표시로 판명, 3차 개정에서 자가진단형으로 전환했습니다: 스폰한 파티클을 보관하고 나중 시점에 비동기 모델 클론 완료 여부·씬 그래프 부모·컬링 플래그·실제 월드 변환을 로그로 역보고(`probe inspect`)하며, 디스크 exe가 SteamStub 암호화라 확인 불가였던 Spawn 함수의 런타임 해석 주소(RVA)도 함께 기록합니다(실측 결과 주소 라이브러리와 정확히 일치 — 주소 이론 종결). 4차 개정에서 검사 타이밍을 SKSE 태스크 체인(큐가 한 번에 드레인되어 전 체크포인트가 스폰 프레임에 몰리는 결함 실측)에서 실시간 타이머 스레드(0.5/2/5/9초)로 교체하고, cloneTask 생성 여부 관측과 MuImpactFramework식 `ProcessLists::globalTempEffects` 수동 등록 변형 2종을 추가했습니다. 그 실측으로 실패 기전이 확정됐습니다: 정적 월드 메시(곰덫)는 클론돼도 월드 변환이 적용되지 않고(원점 고정, lifetime 1초 강제) 아트 오브젝트류 FX(소울트랩)는 건강하게 부착되어도 시퀀스를 매직 시스템이 구동해줘야 해서 보이지 않습니다. 5차 개정에서 그 반대 부류인 스스로 루프 재생하는 룬 프로젝타일 글리프 3종(화염/냉기/전격)과 지면 하자드 NIF도 검사했습니다. 등록 변형은 가시성과 무관하고 이중 갱신만 유발해 제거했습니다.
- QA 디버그 도구를 확장했습니다(디버그 토글이 켜진 경우에만 표시). "선택 베이스에 함정 어픽스" 버튼은 Prisma 패널에서 선택한 무기에 함정 어픽스 6종(곰덫→룬 함정→역병 포자→타르 역병→흡수 포자→혼돈의 룬)을 누를 때마다 하나씩 부여해, 안정성 시나리오(S1/S3)의 장비 준비를 자연 파밍 없이 수십 초로 줄입니다. 스타트 재련 오브 버튼은 디버그 세션에서 1회성 제한 없이 반복 지급됩니다. 부여된 어픽스는 정식 경로와 동일하게 저장·표시·발동됩니다.
- 기존 파티클 렌더 진단을 `Production Trap World-Ref Probe`로 교체했습니다. 런타임 레지스트리의 함정 6종을 실제 `trapFeedback` 정의에서 해석해 플레이어 앞 두 줄에 배치하고, 라이브 함정과 같은 월드 레퍼런스 생성·곰덫 애니메이션 재시도·TrapSystem 정리 경로를 사용합니다. 프로브는 약 3초 뒤 발동 전에 만료되므로 게임플레이 주문을 적용하지 않으며, 중복 실행·분리된 셀·비활성 TrapSystem·논리/물리 마커 상한 부족 시 안전하게 거부됩니다. 로그 검사기는 각 함정의 엔진 레퍼런스 해석 여부를 `OBSERVED 6/6`으로 집계하되 화면 표시 성공으로 과장하지 않습니다. 여섯 표시물과 곰덫을 포함한 자동 정리는 인게임에서 확인했습니다.

### Changed

- **Prisma 작업 화면을 룬워드와 아이템 어픽스의 실제 기능 경계에 맞춰 분리했습니다.** 탭 위의 공통 작업 베이스가 룬워드·재련·초기화의 권위 대상을 명확히 표시하고, 룬워드 탭은 레시피 탐색·변환에, 아이템 어픽스 탭은 장착 빌드·현재 어픽스·보호 재련에 집중합니다. 전체 초기화는 고급 탭으로 이동했습니다. 넓은 화면에서는 패널 전체 스크롤 대신 레시피·효과·보유량처럼 길이가 가변적인 목록만 내부 스크롤합니다.
- **자주 사용하는 아이템 어픽스를 첫 기본 탭으로 배치했습니다.** 메인 탭과 좌우 키보드 이동 순서는 `아이템 어픽스 → 룬워드 → 고급`이며, `Home`도 아이템 어픽스로 이동합니다.
- **공통 작업 베이스 선택창을 화면 크기에 맞춰 확대했습니다.** 넓은 화면에서는 패널 폭의 약 60%를 쓰는 3열 선택창, 중간 화면은 2열, 좁은 화면은 패널 안쪽 전체 폭의 1열로 열리며, 현재 착용 후보를 내부 스크롤 없이 한 번에 펼칩니다. 장비명 글자와 선택 행을 키우고 긴 이름도 생략하지 않으며, 현재 베이스 체크 표시, 첫 `Esc`로 선택창만 닫기, 선택 후 버튼으로 포커스 복귀를 추가했습니다.
- 시체 폭발과 소환 시체 폭발의 속도 제한·연쇄·처리 완료 시체 상태를 `CombatRuntimeState` 소유로 이동하고 설정 재적용·로드·Revert 초기화를 한 경로로 통합했습니다. 판정 순서와 발동 수치는 변경하지 않았습니다.

### Build

- 현재 코세이브 reader 중 LSBG v2(전리품 셔플 백)와 MFLG v1(마이그레이션 플래그)이 production load 경로와 golden roundtrip 테스트에서 같은 payload reader를 사용하도록 연결했습니다. LSBG의 부분 복구·크기 상한·커서 보정과 MFLG의 truncation 처리는 유지되며, 저장 바이트와 레코드 버전은 변경하지 않았습니다.
- 공개 효과 문서 4종의 버전·날짜를 CMake와 CHANGELOG에서 결정적으로 가져오고, `tools/update_docs.py --check`가 임시 디렉터리 재생성 후 바이트 단위 drift를 검사합니다. 이 검사는 일반 CI와 태그 릴리스 모두에서 게시 전 필수로 실행됩니다.
- 최종 DLL과 같은 링크에서 생성된 PDB를 공개 릴리스 자산과 분리된 GitHub Actions 심볼 산출물로 90일간 보존합니다. 공개 자산은 기존과 같이 DLL·ESP·MO2 ZIP·ZIP SHA256 네 개만 유지합니다.

### Compatibility

- rc3는 rc2의 ESP FormID와 코세이브 wire format을 유지하며 기존 세이브와 호환됩니다. 전리품 드롭률과 발동 확률은 변경하지 않았습니다. 보호 재련의 오브 2개 비용과 기존 일반 어픽스 개수 보존은 의도된 동작 변경입니다.
- MO2에서는 이전 버전에 **Merge가 아니라 Replace**로 설치해야 합니다.

## [1.7.4] - 2026-08-09

v1.7.4는 "발동이 보이고 들리는" 릴리스입니다. 커스텀 발동 이펙트가 게임에서 전혀 렌더되지 않던 ESP 계약 결함(MODL 경로·ARTO 타입)을 실측 기반으로 수리하고, CastSpell 계열 발동 105종 전부에 테마별 시청각 피드백을 부여했으며, 다중 어픽스 발동 페널티를 "실제 발동 가능한 어픽스 수" 기준으로 재정의했습니다. Prisma 패널 UX 1차 개선(어픽스 탭 즉시 표시, 미리보기 한글화·박스 분리, 디버그 도구 게이팅, MCM 단축키 즉시 반영)과 크래시 포렌식 인프라(PDB 아카이빙, 상세 로그 주기 flush)를 포함합니다. rc1은 위 변경 전체가 개발 인게임 검증을 통과한 첫 공개 테스트 빌드입니다.

### Fixed

- 커스텀 발동 이펙트(ARTO) 26종이 게임에서 전혀 렌더되지 않던 문제를 수정했습니다. 원인은 두 겹이었습니다. MODL 경로가 `Meshes\` 접두사를 달고 있어 엔진이 `Data\Meshes\Meshes\...`를 찾았고(모델 로드가 소리 없이 실패), Mutagen 0.52.0의 `MagicHitEffect` enum이 raw DNAM=2 — 게임 규약상 Enchantment Effect — 로 직렬화되어 레코드 타입 자체가 잘못 기록됐습니다. Skyrim.esm의 ARTO 201개를 실측해 규약(0=Magic Casting, 1=Magic Hit Effect, 2=Enchantment Effect)을 확정했고, 생성기가 Data\Meshes 상대경로와 raw DNAM=1을 강제하며, 저장된 ESP 바이트를 Mutagen 없이 직접 파싱하는 회귀 테스트로 고정했습니다. 룬 조각과 재련 오브의 인벤토리 모델 경로도 같은 접두사 문제를 수정했습니다.
- 룬워드 꿈의 타격 아트가 월드 폭발용 NIF(`ExplosionShock01`)를 액터 부착 아트로 쓰던 것을 바닐라 액터 부착 전격 아트(`FXShockCloak01`)로 교체했습니다. 명시적 피드백 아트를 발동 시각의 단일 권위로 삼도록 해당 시전은 `noHitEffectArt=true`로 조정했습니다.

### Fixed (UI)

- 패널 UX 1차 개선: ① 아이템 어픽스 탭이 룬워드 베이스로 선택된 아이템의 어픽스를 즉시 표시하고, 빈 화면 안내를 "인벤토리(Tab)를 열어 아이템을 가리키세요"라는 절차형 문구로 바꿨습니다. ② 레시피 미리보기에서 개발용 레코드명("Calamity: Runeword Nadir -> Calamity: Runeword Nadir")이 노출되던 것을 언어 모드에 맞는 표시명으로 정리했습니다. ③ 치트성 디버그 도구(테스트 아이템 지급 등)는 디버그 토글(HUD 알림 또는 상세 로깅)이 켜진 경우에만 고급 탭에 표시됩니다. ④ "부족한 룬조각" 목록이 한 화면에 네 번 반복되던 것을 우측 검토 영역 한 곳으로 정리하고, 베이스 목록의 별표(*)가 어픽스 수를 뜻한다는 범례와 수동 모드 설명을 추가했으며, 엔진이 보내는 상태 문구("Prisma panel opened." 등)를 언어 설정에 맞춰 표시합니다.
- 레시피 미리보기의 효과 이름이 언어 설정을 따릅니다. ESP에는 설계상 ASCII 이름만 기록되므로(일부 UI 경로의 한글 렌더링 제약), 런타임이 config의 "영문 / 한글" 병기 이름을 로드 시 FormID 맵으로 수확해 미리보기가 그쪽을 사용합니다. 한글 절반이 없던 내부 함정 레코드 20종의 이름도 보충하고 표기가 어긋나던 영문 명칭 5종을 정리했습니다.
- 함정 6종이 장전 완료(armed) 시 곰덫 재장전 클릭음을 재생합니다. 함정이 깔린 것을 화면 확인 없이도 인지할 수 있게 하는 시인성 개선입니다.
- 새로 설치한 뒤 MCM에서 패널 단축키를 바꿔도, 패널을 수동으로 한 번 열기 전까지 새 단축키가 작동하지 않던 문제를 수정했습니다. 단축키 캐시를 다시 읽는 워커가 관련 메뉴나 패널이 열린 적이 없으면 잠들어 있어, MCM(저널 메뉴)에서 저장한 키가 반영되지 않았습니다. 이제 저널 메뉴가 닫힐 때 단축키와 UI 언어 설정을 즉시 다시 읽습니다 — MCM 변경이 게임으로 돌아오는 순간 반영됩니다.

### Changed

- 발동 페널티 단계에서 "실제로 발동 가능한" 어픽스만 세도록 판정을 한 번 더 정교화했습니다. 접미사가 아니어도 발동 확률이 0이거나 알림 전용 액션인 어픽스(룬워드 패시브 프리픽스, 스크롤 숙련 단계, 내부 함정 보조 항목 15종)는 이제 같은 아이템의 발동 어픽스 확률을 깎지 않습니다. 장착 시 실계산과 툴팁 추정이 같은 판정을 공유합니다.
- 다중 어픽스 발동 페널티(Best Slot Wins)의 단계 산정을 "아이템의 전체 효과 수"에서 "아이템의 발동형(비접미사) 어픽스 수"로 바꿨습니다. 기존 방식은 발동과 무관한 패시브 접미사까지 세어, 발동 어픽스 1개에 패시브 3개가 실린 구성이 발동 4중첩과 동일한 ×0.5를 받았고, 기존 어픽스를 보존하는 룬워드 완성 아이템이 상시 이 구간에 떨어졌습니다(표기 30% / 실제 15%). 이제 발동형 수만 단계에 반영되며, 장착 시 실계산과 툴팁 표시가 같은 함수를 공유해 어긋날 수 없습니다. **장비 구성에 따라 발동 빈도가 올라가는 의도된 밸런스 상향입니다.** 문서에 없던 4효과 단계(50%)를 포함해 넥서스 설명도 코드와 일치시켰습니다.

### Added

- CastSpell 계열 발동 105종(코어 34, 룬워드 71) 전부에 테마별 시청각 피드백을 추가했습니다. 즉시형 발동은 투사체도 시전 아트도 없고 지속 0초 히트 셰이더는 표시되지 않아, 지금까지 화면·소리 모두 없이 발동돼 왔습니다. 원소 타격은 해당 원소 cloak 아트, 흡수류는 색상 규약(적=생명력, 녹=기력, 청=매지카)의 흡수 아트, 정신 계열은 일루전 바디 아트, 소환은 소환 아트와 소환음을 사용합니다. 모든 아트는 Skyrim.esm에서 액터 부착형(raw DNAM=1)으로, 모든 사운드는 SNDR 레코드로 실측 검증된 바닐라 자산입니다. lint와 생성기 테스트가 이후 추가되는 CastSpell 어픽스에도 피드백 선언을 강제합니다.
- 발동 파이프라인 관측성을 추가했습니다: 자체 마법 효과의 엔진 적용 이벤트 관찰 로그, 피드백 아트/사운드의 엔진 수락 결과 로그, 시전 직후 체력 표본 로그(같은 호출 내 표본이며 피해 성패 판정이 아님을 로그 문구에 명시).
- 아이템 툴팁이 발동형 어픽스의 기본 발동률과 함께 현재 장착 기준(또는 단독 장착 추정) 유효 발동률을 표시합니다.
- 물리 피해 전환(주입) 효과 3종에 적용 시 히트 사운드(SNDD On Hit)를 추가했습니다 — 화염 `MAGFireboltImpactSD`, 냉기 `MAGFrostBiteImpactSD`, 전격 `MAGShockImpactSD`. 바닐라가 시전 사이클 없는 적용(물약·의복 인챈트)에 쓰는 슬롯과 같은 방식입니다. 화염 1종을 프로토타입으로 먼저 배포해 인게임 재생을 확인한 뒤 냉기·전격으로 확대했으며, 전격 대미지 주문을 함께 쓰는 아크메이지 효과도 같은 사운드를 얻습니다. Mind over Matter는 주문을 시전하지 않는 리다이렉트라 대상이 아닙니다.
- QA 도구와 문서: `tools/qa_skse_logcheck.py`에 발동 파이프라인 관찰 섹션을 추가했습니다 — 엔진 경로 관찰 전용으로 판정(PASS/FAIL)에 관여하지 않으며, 화면 표시나 음향 성공을 의미하지 않습니다. 인게임 스모크 체크리스트(`docs/QA_SMOKE_CHECKLIST.md`)와 v1.7.3 안정성 수정 3건의 검증 시나리오 매트릭스(`docs/QA_STABILITY_SCENARIOS.md`)를 문서화했습니다.

### Build

- 릴리스 DLL이 CodeView 디버그 정보(/Z7)와 링커 PDB를 함께 생성하고, 패키징 스크립트가 PDB를 ZIP 옆에 보관합니다. 크래시 도구는 `Module+Offset`만 남기므로, 대응 PDB가 있어야 함수 단위 역추적이 가능합니다(2026-07-23 크래시가 미제로 남은 원인). `/DEBUG`가 조용히 꺼버리는 `/OPT:REF,ICF`는 명시적으로 복원해 배포 바이너리 최적화를 유지했습니다.
- 상세 로깅이 켜진 세션은 5초 주기로 로그를 flush합니다. 프리징이나 강제 종료 시 전투 로그 꼬리가 통째로 유실되던 문제(2026-08-07 사례)의 완화이며, 상세 로깅이 꺼진 일반 세션에는 주기 flush가 붙지 않습니다.

### Compatibility

- 코세이브 직렬화 형식과 ESP FormID는 변경하지 않았으며 기존 세이브와 호환됩니다. 페널티 산정 변경은 장착 재계산 시 자동 반영됩니다.
- 발동 페널티 변경과 105종 피드백 추가는 의도된 체감 변경입니다. 시각·음향의 실제 표시와 잔류 유무, v1.7.3 안정성 수정 3건(함정 셀 detach, 트리거 인덱스 스냅샷, 함정 틱 통합)의 크래시 부재는 위 QA 문서 절차에 따라 인게임에서 확인해야 합니다.

## [1.7.3] - 2026-07-29

v1.7.3은 CTD 2건과 어픽스 발동이 영구히 멈추는 결함 1건을 수정하는 안정성 릴리스입니다. rc3는 rc2의 Prisma 패널 분할 회귀와 릴리스 검증 공백을 수정했고, rc4는 효과 설명과 실동작의 정합성, ESL 환경의 MCM 연결, 완성 룬워드 재련, 런타임 상태 전이와 생성·릴리스 검증 계약을 보강합니다. v1.7.2는 태그되지 않았으므로 이 릴리스에는 v1.7.1 이후의 모든 변경이 포함됩니다.

### Fixed

- 어픽스 발동이 어느 시점부터 전부 멈추던 문제를 수정했습니다. 발동 깊이 카운터를 수동으로 증감하고 있어 중간에 예외가 전파되면 카운터가 양수로 남았고, 이후 모든 발동이 가드에 걸려 차단됐습니다. RAII 가드로 교체해 어떤 경로로 빠져나가도 반드시 복원됩니다.
- 셀이 언로드된 뒤 함정 이펙트를 생성하거나 해제하면서 발생하던 CTD를 수정했습니다. 함정이 소속 셀 포인터를 그대로 들고 TTL 동안 재사용하고 있어, 셀 언로드 시 해제된 메모리를 참조했습니다.
- 어픽스 발동 처리 도중 발생하던 CTD를 수정했습니다. 발동 대상 목록을 순회하는 동안 발동 효과가 엔진에 재진입하고, 그 과정에서 장착 이벤트가 같은 목록을 재구축하면 순회 중이던 메모리가 무효화됐습니다. 순회 시작 시점의 사본을 사용하도록 바꿔 재구축과 무관하게 완료됩니다.
- v1.7.3-rc2에서 Prisma 패널을 CSS/JS 파일로 분리한 뒤 `state.js`가 뒤쪽 `tooltip.js`의 함수를 먼저 호출해 초기화가 중단되던 문제를 수정했습니다. 실제 HTML 태그 순서대로 각 classic script를 별도로 실행하는 회귀 테스트를 추가해 단일 문자열 결합이 브라우저 의미를 가리는 문제도 막았습니다.
- Storm Call, Flame Strike, Frost Strike, Spark Strike, Ember Brand가 설명의 고정 피해를 적용하지 않고 비율 피해만 주던 문제를 수정했습니다. 이제 각각 `10 + 10%`, `6 + 10%`, `6 + 10%`, `6 + 10%`, `4 + 4%`의 물리 타격 기반 원소 피해를 적용합니다.
- 7개 Crit Cast 효과가 주문의 가장 큰 부가 수치를 피해량으로 잘못 선택할 수 있던 문제를 수정했습니다. 즉시 적용되는 직접 원소 생명력 피해만 기준으로 삼고, 그 값과 물리 타격 피해의 `30%` 중 큰 값을 사용합니다. 근접 무기는 치명타/강공격, 활·석궁은 강공격이 없으므로 모든 적중에서 판정한다는 설명도 실제 동작과 일치시켰습니다.
- ESL로 로드된 플러그인의 MCM 퀘스트를 전체 `FE...` FormID로 다시 조회해 등록에 실패할 수 있던 문제를 수정했습니다. light 플러그인 로컬 FormID의 하위 12비트로 정규화합니다.
- 완성 룬워드 재련이 룬워드 토큰이나 진화 XP·모드 상태까지 건드릴 수 있던 문제를 수정했습니다. 일반 어픽스만 다시 굴리고 룬워드와 Grief/Infinity 상태를 보존하며, 재련 오브가 정확히 1개 소모되지 않으면 가능한 범위에서 보상 복구 후 실패합니다.
- 실패 가능한 발동 준비가 완료되기 전에 확률·예산·ICD·XP·상태를 확정하던 경로를 사전 준비 단계로 이동했습니다. 저장/불러오기 세대가 바뀐 비동기 피해 작업과 유효하지 않은 셀의 함정 작업도 기존 상태를 오염시키지 않고 폐기합니다.

### Changed

- 함정 틱에서 주변 액터 목록을 함정마다 한 번씩 훑던 것을 틱당 한 번으로 통합했습니다. 함정이 많을수록 비용이 함정 수에 비례해 늘어나던 부분이 개선됩니다. 생존·적대·거리 판정은 함정별로 그대로 수행하므로, 앞선 함정이 처치한 대상은 뒤 함정에서 이전과 동일하게 제외됩니다.
- EventBridge의 참조 별칭 14개를 제거하고 트리거 캐시 소유권을 명시적인 상태 그룹으로 이동했습니다. 런타임 활성 상태는 원자적으로 읽고 쓰며, 비동기 작업은 저장/불러오기 세대로 유효성을 확인합니다.
- Prisma 재련 안내와 호환성 메시지가 완성 룬워드의 “일반 어픽스만 재련” 계약을 그대로 표시하도록 C++ payload와 화면 문구를 동기화했습니다.

### Build

- 릴리스 워크플로가 GitHub 러너에서 패키징할 수 있게 됐습니다. Papyrus 컴파일러는 Creation Kit에 딸려 오는 로컬 전용 바이너리라 러너에 설치할 수 없어, 커밋된 `.pex`를 대신 사용합니다. 이때 `tools/papyrus.pin.json`이 그 `.pex`가 현재 `.psc`·스텁·플래그 파일에서 나온 것임을 해시로 증명하며, 어긋나면 패키징을 중단합니다 — `compile_papyrus.sh`의 "실패한 컴파일을 기존 PEX로 대체하지 않는다"는 규칙을 유지하기 위한 조건입니다. 폴백은 컴파일러가 *없을 때만* 작동하고, 컴파일 실패는 이전과 동일하게 릴리스를 중단시킵니다.
- `.psc` 수정 후 재컴파일을 잊는 경우를 `python3 tools/verify_papyrus_pin.py`가 린트 단계에서 잡습니다. 핀 갱신은 `--update`로 하며, 이 옵션은 해시를 기록하기 전에 직접 재컴파일합니다.
- 같은 태그의 GitHub 릴리스가 이미 있을 때 무조건 성공하지 않고, 원격 태그 commit·draft/prerelease 상태·4개 공개 자산의 이름과 SHA256이 이번 빌드와 모두 일치할 때만 무변경 성공하도록 강화했습니다. 불일치·누락은 기존 자산을 덮어쓰지 않고 릴리스를 중단합니다.
- 실제 DLL이 준비된 runtime-gate CI job에서 Papyrus 컴파일러 부재 패키징과 핀 실패 거부 E2E를 필수 실행합니다. DLL이 없으면 더 이상 조용히 skip하지 않습니다.
- 어픽스 action 스키마를 실제 런타임 계약에 맞게 엄격하게 제한하고, source/generated JSON의 정확한 동기화와 KID/SPID 산출물의 결정성을 검증합니다. 릴리스 워크플로는 실제 DLL 빌드 뒤 Prisma·런타임 계약과 패키징 E2E를 모두 통과해야 게시 단계로 이동합니다.

### Compatibility

- 코세이브 직렬화 형식, MCM 옵션, ESP 레코드와 드랍 확률은 변경하지 않았으며 새 게임은 필요하지 않습니다.
- 위 5개 타격 피해 효과와 7개 Crit Cast의 설명·피해 선택 계약은 의도적으로 변경됐습니다. 기존 장비와 완성 룬워드는 그대로 로드되며 재련 시 룬워드·진화 상태를 보존합니다.
- 정적 분석, 생성기·도구·런타임 회귀 테스트와 실제 패키징 검증은 완료했습니다. Skyrim에서의 피해 수치, 구세이브 MCM 재등록, 완성 룬워드 재련과 전투 중 저장/불러오기는 공개 RC에서 확인해야 합니다.

## [1.7.2] - 2026-07-27

v1.7.2는 v1.7.1의 보스 시체 화폐 보상 등급 판정 중 발생할 수 있는 CTD를 수정하고, 반복 소비되는 재련 오브의 공급 비중을 높이는 핫픽스입니다.

### Added

- 첫 Calamity 전용 NIF 프로토타입으로 `Meshes\CalamityAffixes\VFX\RuneTrapMarker_Calamity.nif`를 추가했습니다. 스카이림 기본 효과 텍스처만 사용하며 바닐라 레코드를 덮어쓰지 않습니다.

### Changed

- `rune_trap` 지속 마커 아트를 바닐라 `RuneFrostProjectile01.nif`에서 위 전용 메시로 교체했습니다. 지면 가독성·방향·밀도·저장/로드 정리는 인게임 smoke test 대상으로 남습니다.

- 일반 적의 독립 판정을 룬워드 조각 `8%`, 재련 오브 `12%`로 조정했습니다. 두 통화는 같은 시체에서 함께 획득할 수 있습니다.
- 고유·네임드 적의 단일 확정 보상 분배를 룬워드 조각 `40%`, 재련 오브 `60%`로 조정했습니다.
- 보스의 룬워드 조각 1개와 재련 오브 1개 확정, 기존 피티·중복 방지·룬 가중치는 유지했습니다.

### Fixed

- `LocRefTypeBoss` 조회가 벤더 CommonLib의 잘못된 기본 오브젝트 초기화 플래그 접근을 통과하지 않도록 우회했습니다.
- 보스 우선, 네임드 고유 차선의 보상 등급과 기존 보장 보상·중복 방지·피티 비간섭 계약은 유지했습니다.
- 보스 판정 경로에 위험한 `BGSDefaultObjectManager::GetObject()` 호출이 다시 들어오지 않도록 런타임 회귀 게이트를 추가했습니다.

### Compatibility

- 기존 `user_settings.json`에 저장된 MCM 확률은 사용자 선택을 보존하기 위해 계속 우선합니다. 기존 설치에서 새 권고값을 사용하려면 MCM에서 룬 조각 `8`, 재련 오브 `12`로 설정해야 합니다.

## [1.7.1] - 2026-07-20

v1.7.1은 적격 네임드 고유 적과 보스에게 룬워드 조각·재련 오브 보상을 보장하는 공개 테스트 릴리스입니다.

### Added

- Actor Base가 `Unique`인 적은 통화 카테고리가 모두 사용 가능할 때 룬워드 조각 또는 재련 오브 중 정확히 하나를 확정 획득합니다. 기본 비율은 룬워드 조각 `70%`, 재련 오브 `30%`입니다.
- `LocRefTypeBoss`가 지정된 보스는 룬워드 조각 1개와 재련 오브 1개를 각각 확정 획득합니다.
- 네임드 고유·보스 보상 분류, 우선순위, 카테고리별 중복 방지와 피티 비간섭 계약을 검증하는 런타임 회귀 테스트를 추가했습니다.

### Changed

- 보스 분류는 네임드 고유 분류보다 우선하며, 보장 보상 대상은 같은 시체에서 일반 `12% / 7%` 확률 판정을 추가로 수행하지 않습니다.
- 네임드 고유 보상의 룬워드 조각 선택 비율을 데이터 설정 `loot.uniqueActorGuaranteedRunewordChancePercent`로 분리했습니다.

### Compatibility

- 일반 적의 룬워드 조각 `12%`, 재련 오브 `7%`, 룬 조각 99회 무드롭 피티와 기존 룬 가중치는 변경하지 않았습니다.
- 보장 보상은 기존 eligible hostile corpse-only 판정, 카테고리 ledger와 legacy SPID 중복 감지를 그대로 통과하며 일반 피티를 증가시키거나 초기화하지 않습니다.
- 코세이브 직렬화 형식, MCM 옵션, ESP 레코드와 공개 효과 계약은 변경하지 않았으며 새 게임은 필요하지 않습니다.
- 네임드·보스 분류와 실제 획득 체감은 인게임 공개 테스트가 필요합니다.

## [1.7.0] - 2026-07-17

v1.7.0은 시체 기반 통화 획득의 기본 체감 빈도를 높이고, Prisma 룬워드 패널을 실제 패널 너비에 맞는 작업 흐름으로 정리하는 공개 테스트 릴리스입니다.

### Added

- 레시피 검색과 함께 AND 조건으로 적용되는 무기·방어구·혼합 베이스 필터를 추가했습니다.
- 필터의 선택·언어·접근성 상태와 keyed recipe DOM 재사용을 고정하는 UI 동작 회귀 검증을 추가했습니다.

### Changed

- 적격 적 시체당 룬워드 조각 기본 드랍 확률을 `8%`에서 `12%`로, 재련 오브를 `5%`에서 `7%`로 조정하고 Generator·SKSE·MCM·Papyrus 기본값과 현재 문서를 동기화했습니다.
- Prisma 룬워드 작업대를 패널 너비에 따라 3열·2열·1열로 전환하고, 중복 진행 표시를 세 작업 카드의 상태로 통합했습니다.
- 레시피 검색과 함께 사용할 수 있는 무기·방어구·혼합 베이스 필터를 추가하고, 변환·아이템 작업·복구 및 초기화 명령을 역할별로 분리했습니다.

### Compatibility

- 기존 `user_settings.json`에 저장된 사용자의 룬 조각·재련 오브 확률은 계속 우선 적용되며, `12% / 7%`는 새 설치 또는 해당 사용자 override가 없는 경우의 기본값입니다.
- 통화 판정 권한은 계속 eligible hostile corpse-only 런타임 경로이며, 99회 룬 무드롭 피티·룬 가중치·시체 중복 방지 ledger는 변경하지 않았습니다.
- 패널 명령 ID, C++↔JS payload, MCM 언어 기본값, 패널 위치·크기 저장 형식과 게임플레이·직렬화 계약을 변경하지 않았습니다.
- v1.6.0 VFX와 v1.7.0 패널의 실제 인게임 시각·장기 플레이 검증은 공개 테스트에서 계속 확인해야 합니다.

## [1.6.0] - 2026-07-17

v1.6.0은 게임플레이 수치와 발동 계약을 유지하면서 함정, 시체 폭발, 방어·긴급 발동의 위치와 상태를 알아볼 수 있도록 바닐라 자산 기반 VFX/SFX를 추가하는 공개 테스트 릴리스입니다.

### Added

- Bear Trap, Rune Trap, Plague Spore, Tar Blight, Siphon Spore, Chaos Rune에 설치·무장·발동·자연 만료 피드백과 세계 좌표 고정 마커를 추가했습니다.
- 화염 6종과 역병 2종 시체 폭발에 시체 중심 custom ArtObject와 위치 기반 일회성 사운드를 추가했습니다.
- 긴급 회복, 은신·위상, 물리 방벽, 마법·원소 방벽, 반사·가시, 광휘, 용의 비늘로 구분되는 방어 발동 VFX/SFX 7계열을 추가했습니다.
- `feedback.target=Corpse`, `feedback.spatialSound`와 SpawnTrap 전용 `trapFeedback` 데이터 계약, JSON Schema·Generator·Python lint 회귀 검증을 추가했습니다.

### Changed

- 함정 마커를 임시 object reference 대신 `BSTempEffectParticle`로 실제 판정 중심에 생성하고, 상태 전이에서만 교체하도록 구현했습니다.
- 함정의 만료·소모·cap 축출·잘못된 상태·설정/로드 reset 제거를 중앙 정리 경로로 통합했습니다.
- 시체 폭발 custom ARTO가 해석되면 기존 공통 셰이더를 중복 재생하지 않고, 해석 실패 시 기존 셰이더를 fallback으로 유지합니다.
- 신규 ArtObject 21개를 `0xAEB..0xAFF`에 append-only로 추가해 총 major record 수를 768개, Next Object ID를 `0xB00`으로 갱신했습니다.

### Compatibility

- v1.5.0의 첫 747개 레코드는 FormID, signature, EditorID와 raw record payload를 그대로 유지합니다.
- 공개 효과 243개의 수치, 발동률, ICD, 타깃, 피해 계산, 함정 판정, 룬 가중치, 드롭 정책과 직렬화 토큰을 변경하지 않았습니다.
- 신규 VFX/SFX 실패는 주문 시전, RNG, proc/cast budget, trigger count와 재장전 상태에 영향을 주지 않습니다.
- 활성 함정 저장·로드 후 ghost particle, 바닐라 NIF 지면 가독성, 위치 음향과 48개 마커 밀도는 인게임 공개 테스트가 필요합니다.

## [1.4.0] - 2026-07-14

v1.4.0은 룬워드 조각과 재련 오브의 획득 권한을 플레이어 측 적대 처치의 시체로 일원화하고, 피티·시체 중복 방지를 저장하는 공개 테스트 릴리스입니다. Dragon 효과 레코드 분리, 룬워드·MGEF 생성 계약 강화, Prisma 레시피 상세와 접근성 개선, fail-hard 릴리스 검증도 함께 포함합니다.

### Added

- 룬 조각/재련 오브 피티 카운터와 시체 FormID·날짜·카테고리 처리 mask를 저장하는 추가 코세이브 레코드 `CCRT v1`을 도입했습니다.
- 플레이어/player-owned summon·proxy 적대 처치, 제외 대상, 카테고리별 전환 중복 방지와 corpse-only 소스 계약을 고정하는 런타임 회귀 검증을 추가했습니다.
- Prisma 레시피의 한국어/영어 상세 payload, viewport clamp, wheel·scrollbar·키보드 스크롤 및 ARIA 상태를 검증하는 UI 계약을 추가했습니다.

### Changed

- 룬 조각과 재련 오브의 실제 판정 권한을 SKSE `TESDeathEvent` 기반 **eligible hostile corpse-only** 경로로 일원화했습니다. 성공한 통화는 월드에 생성하지 않고 사망한 적의 인벤토리에 조용히 직접 추가합니다.
- 일반 상자/컨테이너 활성화, 아이템 픽업, 월드 `PlaceAtMe`, 플레이어 인벤토리 직행과 새 SPID 통화 분배를 비활성화했습니다. `loot.currencyDropMode=hybrid`는 구 설정 호환 토큰으로만 유지합니다.
- 피해자가 팔로워/동료, 소환·지휘 액터, 아동, player-owned 또는 비적대 대상인 경우와 환경 오브젝트·player-owned가 아닌 독립 NPC/팔로워의 처치를 통화 판정에서 제외했습니다.
- `Data/CalamityAffixes_DISTR.ini`를 빈 호환 산출물로 전환하고 SPID를 권장 의존성에서 제거했습니다.
- 룬워드 레시피의 효과·권장 베이스·룬 순서·상세를 선택 언어에 맞춰 검색 결과, 선택 상태와 hover에서 일관되게 표시하도록 Prisma payload를 확장했습니다.
- Prisma 패널을 viewport 안으로 제한하고 레시피 탐색기의 네이티브 scrollbar·키보드·ARIA 상호작용을 보강했습니다.
- Dragon의 화염/냉기/전격 저항을 Chains of Honor와 공유하던 MGEF에서 분리해 전용 append-only MGEF 3개를 사용하도록 변경했습니다.
- 생성기가 정확한 **94개 룬워드 + 33개 룬 가중치** 계약, 중복 ID와 생성 레코드 충돌에서 즉시 실패하도록 검증을 강화했습니다.
- MO2 패키징이 방금 빌드한 DLL과 실제 Papyrus 컴파일 결과만 받도록 하고, ZIP의 정확한 PEX 3종·`runtime_contract.json`·버전·해시가 맞지 않으면 릴리스 검증이 실패하도록 변경했습니다.

### Fixed

- 전투와 무관한 일반 상자/컨테이너에서 통화가 판정되거나, 월드에 갑자기 튀어나와 보일 수 있던 부자연스러운 드랍 경로를 제거했습니다.
- 같은 시체에 death event가 다시 전달되거나 저장을 다시 불러왔을 때 룬 조각/재련 오브 판정이 중복될 수 있던 경로를 카테고리 ledger로 차단했습니다.
- 레시피 상세가 선택 언어나 표시 위치에 따라 축약·누락되고, 패널이 화면 밖으로 벗어나거나 스크롤 입력 상태가 보조기술에 전달되지 않던 UI 계약을 보완했습니다.

### Compatibility

- 기본 룬 조각 `8%`, 재련 오브 `5%`, 룬 가중치 `El-Amn=4 / Sol-Um=3 / Mal-Lo=2 / Sur-Zod=1`과 99회 룬 무드롭 피티는 유지합니다. 피티는 이제 `CCRT`에 저장됩니다.
- 업데이트 전에 SPID로 통화 또는 해당 레벨드 리스트가 들어간 전환 시체는 카테고리별로 감지해 같은 카테고리만 건너뜁니다.
- 기존 어픽스·룬워드 ID와 생성 레코드 순서를 유지하고 Dragon 전용 MGEF 3개만 append-only 영역에 추가합니다.
- 기존 장비, 보유 룬 조각·재련 오브, 완성 룬워드와 기존 코세이브 직렬화 레코드는 그대로 로드됩니다. `CCRT`는 추가 레코드이므로 새 게임이 필요하지 않습니다.

## [1.3.3] - 2026-07-14

v1.3.3은 v1.3.2의 패널 성능 개선을 유지하면서, 레시피 탐색기의 커스텀 wheel RAF가 네이티브 scrollbar 조작과 경쟁하던 회귀를 바로잡는 공개 테스트 hotfix입니다. 실제 게임 테스트에서 수정 후 스크롤이 부드러워진 것을 확인했습니다.

### Added

- 실제 index.html의 스크롤 controller를 추출해 가짜 CEF DOM, 정수형 scrollTop과 RAF queue에서 실행하는 Node 동작 회귀 테스트를 추가했습니다.
- 마우스 휠 위·아래, legacy deltaY=0, 다중 노치 clamp, line/page mode, 트랙패드, 즉시·지연 thumb 이동과 pointer/mouse 취소를 고정하는 계약을 추가했습니다.

### Changed

- 명확한 120단위 일반 마우스 휠 노치만 한 노치당 64px 목표로 정규화하고, 트랙패드의 작은 연속 pixel delta는 그대로 유지합니다.
- scroll 감쇠 시간상수를 72ms에서 48ms로 줄이고, 0.75px 도달 거리와 0.5px 최소 유효 이동량으로 정수형 CEF에서도 RAF가 확실히 종료되도록 변경했습니다.
- 커스텀 frame-paced wheel 처리를 명시적으로 지정된 레시피 목록에만 한정해 다른 패널 스크롤 영역의 네이티브 동작을 보존합니다.

### Fixed

- 레시피 탐색기의 진행 중 wheel RAF가 scrollbar thumb·track·키보드 또는 외부 scrollTop 이동을 이전 목표 위치로 되감던 문제를 수정했습니다.
- 자체 scroll 이벤트 뒤 native scroll 통지가 한 RAF 늦게 도착하는 경우에도 다음 RAF가 실제 위치를 덮어쓰기 전에 취소·동기화하도록 수정했습니다.
- 일부 CEF 입력 경로에서 일반 마우스 휠이 작은 pixel delta로 전달되어 스크롤이 지나치게 느리던 문제를 수정했습니다.
- 정수 단위로 양자화되는 scrollTop에서 마지막 1px 부근의 RAF가 종료되지 않을 수 있던 문제를 수정했습니다.

### Compatibility

- Prisma UI 프레임워크와 v1.3.2의 Tick 병합, 정적 레시피 분리, keyed DOM, View 복구 및 MCM 안전 폴백을 유지합니다.
- 어픽스·룬워드 효과 정의, 룬 가중치와 분포, 기본 파편 드롭률 8%, 99회 무드롭 피티는 변경하지 않았습니다.
- 기존 장비, 파편, 완성 룬워드와 저장·직렬화 형식은 변경하지 않았으며 새 게임은 필요하지 않습니다.

## [1.3.2] - 2026-07-13

`v1.3.2`는 Prisma UI 프레임워크와 v1.3.1의 View 복구 경로를 유지하면서, 패널을 연 뒤 반복되던 메인 스레드 작업과 거친 입력 처리를 줄여 조작감과 프레임 일관성을 개선하는 공개 테스트 패치입니다.

### Added

- worker Tick 병합, 정적 레시피 카탈로그 분리, 중복 interop 억제와 시간 기반 스크롤 동작을 고정하는 Prisma 패널 성능 회귀 검증을 추가했습니다.

### Changed

- worker가 예약하거나 실행 중인 Prisma Tick을 최대 하나로 제한해, 이전 Tick이 밀린 동안 같은 작업이 SKSE task queue에 중복으로 쌓이지 않도록 변경했습니다.
- 94개 룬워드의 정적 레시피 카탈로그를 주기 갱신에서 분리하고, 선택 항목과 보유 파편·진행 상태 같은 동적 데이터만 필요한 시점에 갱신하도록 변경했습니다.
- 이미 적용된 패널 open 상태와 내용이 같은 interop payload는 다시 보내지 않도록 상태 동기화 경로를 정리했습니다.
- 레시피 목록은 keyed DOM 재사용과 검색용 텍스트 캐시를 사용해, 선택이나 검색이 바뀔 때 전체 항목을 불필요하게 다시 생성하는 비용을 줄였습니다.
- 휠 입력은 고정 픽셀 즉시 점프 대신 delta와 경과 시간을 제한한 시간 기반 스크롤로 처리합니다.
- 패널 상태 강조는 무한 `box-shadow` 애니메이션과 `offsetWidth` 강제 reflow 대신 짧은 compositor 친화적 전환과 `requestAnimationFrame` 단위 갱신을 사용합니다.

### Fixed

- 패널이 열린 동안 300ms 주기 작업이 밀리면 동일 Tick이 계속 예약되어 입력 반응과 프레임이 더 둔해질 수 있던 문제를 수정했습니다.
- 내용이 바뀌지 않았는데도 정적 레시피 설명과 JSON payload를 반복 생성할 수 있던 문제를 수정했습니다.
- 레시피 스크롤이 작은 휠 입력에도 큰 간격으로 즉시 이동해 딱딱하게 느껴지던 문제를 완화했습니다.
- 상태 전환 애니메이션이 동기 레이아웃 계산과 넓은 그림자 repaint를 유발할 수 있던 경로를 제거했습니다.

### Compatibility

- Prisma UI는 전체 툴팁과 룬워드·재련 패널을 위한 기본 프레임워크로 계속 사용하며, v1.3.1의 API/View/DOM-ready 복구와 MCM 안전 폴백을 유지합니다.
- 어픽스 및 룬워드 효과 정의, 룬 가중치와 분포, 기본 파편 드롭률 `8%`, 99회 무드롭 피티는 변경하지 않았습니다.
- 기존 장비, 보유 파편, 재련 오브와 완성된 룬워드는 그대로 유지됩니다.
- 세이브 및 어픽스·룬워드 직렬화 형식은 변경하지 않았으며 새 게임은 필요하지 않습니다.

## [1.3.1] - 2026-07-13

`v1.3.1`은 Prisma UI 프레임워크를 유지하면서 View 생성·DOM 준비·재시도 수명주기를 안정화하고, Prisma를 사용할 수 없을 때도 상태 확인과 안전한 통화 복구를 제공하는 공개 테스트 패치입니다.

### Added

- MCM에 Prisma UI 상태 확인, 룬워드 상태 표시와 누락 통화 복구 기능을 추가했습니다.
- Prisma View identity, 오래된 DOM-ready 콜백, timeout 복구와 MCM 이벤트 연결을 고정하는 회귀 테스트를 추가했습니다.

### Changed

- Prisma API 및 View 생성 실패 시 5초 간격으로 재시도하고, DOM-ready가 15초 동안 도착하지 않으면 해당 View를 폐기한 뒤 복구하도록 변경했습니다.
- Prisma 상태 확인은 정상 View를 강제로 파괴하지 않는 비파괴 검사로 동작합니다.
- 새 View가 생성되면 툴팁, 언어, 레이아웃, 룬워드 목록, 레시피와 패널 캐시를 완전히 갱신합니다.
- 패널 열기 요청은 View 로딩 중에도 보존하며, 실제 DOM-ready 이후에만 패널이 열린 것으로 처리합니다.
- 사용을 종료한 Vibekit 도구와 남아 있던 릴리스 검증 호출을 제거했습니다.

### Fixed

- `CreateView()`가 invalid handle을 반환했는데도 View API를 호출할 수 있던 문제를 수정했습니다.
- invalid nonzero View가 남아 이후 복구를 영구적으로 막을 수 있던 문제를 수정했습니다.
- 이전 View의 늦은 DOM-ready 콜백이 새 View의 준비 상태를 덮어쓸 수 있던 문제를 수정했습니다.
- Prisma 콜백과 활성 View 상태 사이의 동시 접근 및 동기 콜백 손실 가능성을 수정했습니다.
- Prisma가 없거나 View 생성에 실패했는데도 패널이 열렸다는 성공 HUD가 표시되던 문제를 수정했습니다.

### Compatibility

- Prisma UI는 전체 툴팁과 룬워드·재련 패널을 위한 기본 프레임워크로 계속 사용합니다.
- MCM 폴백은 상태 확인과 안전 복구만 제공하며 전체 Prisma UI를 복제하지 않습니다.
- 세이브 및 어픽스·룬워드 직렬화 형식, 효과 정의, 룬 분포와 드롭률은 변경하지 않았습니다.
- 누락 통화 복구는 세이브당 한 번만 동작하며 장비, 어픽스 또는 완성 룬워드를 초기화하지 않습니다.

## [1.3.0] - 2026-07-13

`v1.3.0`은 어픽스와 룬워드의 역할 중복을 줄이고, 각 효과가 실제 빌드 선택으로 구분되도록 발동 구조·접미 중첩·장비 유형·런타임 정확성을 함께 재정비한 공개 테스트 프리릴리즈입니다.

### Added

- 한손, 양손, 활·석궁을 구분하는 무기 하위 유형 계약을 추가해 Swordsman, Champion, Marksman, Eagle Eye 계열이 의미 있는 무기군에 생성되도록 했습니다.
- Strength, Edge, Zephyr, Oath, Myth, Unbending Will에 권장 룬워드 베이스 경고를 추가했습니다. 경고는 비차단 방식이며 플레이어가 원하면 그대로 변환할 수 있습니다.
- Plague, Breath of the Dying 및 일반 시체 폭발이 함께 존재할 때 고유 효과가 영구적으로 가려지지 않도록 시체 폭발 선택 우선순위를 추가했습니다.
- 동일 접미 계열 최고 티어 선택, 무기 하위 유형, 시체 폭발 우선순위와 새 룬워드 의미 계약을 고정하는 회귀 테스트를 추가했습니다.

### Changed

- Destruction, Hand of Justice, Dragon, Mist, Famine, Beast, Eternity, Enigma, Last Wish, Plague, Pride, Mosaic, Obsession 등 주요 룬워드를 단순 수치 변형에서 고유한 복합 발동 효과로 개편했습니다.
- Chaos, Breath of the Dying, Rhyme, Faith, Call to Arms, Honor, Hustle-A를 포함한 기존 룬워드의 시그니처 동작과 설명을 강화했습니다.
- Shadow Stride, Nourishing Flame, Mana Knot, Shock Weakness, Death Mark, Ice Form의 발동 조건과 전투 역할을 구분했습니다.
- 같은 접미 계열을 여러 부위에 장착하면 합산하지 않고 가장 높은 티어 하나만 적용하도록 변경했습니다.
- 접미 22개 계열의 실제 T1/T2/T3 선택 가중치를 `6/3/1`로 통일했습니다.
- Assassin 접미 툴팁에 치명타 확률뿐 아니라 실제 적용되는 치명타 피해 증가도 표시합니다.
- 프리픽스 티어 계열과 원소 피해 주입 계열은 한 아이템에서 같은 패밀리가 중복 선택되지 않도록 변경했습니다.

### Fixed

- hostile Magic Effect의 잘못된 수치 부호, 회복 플래그, Actor Value 연결과 시체 체력 비율 단위를 수정했습니다.
- 모드 효과를 껐다 켰을 때 발동 효과와 접미 패시브가 정확히 중단·복원되도록 런타임 상태 동기화를 보강했습니다.
- 같은 타격이 여러 이벤트 경로에서 중복 처리될 수 있던 문제를 줄이고 함정·룬워드 재변환 경로를 보완했습니다.
- 구버전 저장에 남은 접미 Ability를 실제 Spell 보유 상태와 대조해 불필요한 패시브를 제거하고 누락된 패시브를 복원합니다.
- Plague 시체 폭발이 Death Pyre 계열의 높은 기본 피해 때문에 선택 단계에서 항상 사라지던 문제를 수정했습니다.
- Generator의 전방 Magic Effect 참조와 일부 룬워드의 잘못된 Magic Effect 연결을 제거했습니다.

### Balance

- 공개 프리픽스 73개, 접미 22계열 66개, 룬워드 94개의 역할을 재검토했으며 정규화된 공개 효과 중 완전히 동일한 의미의 중복은 남기지 않았습니다.
- 공격속도, 이동·은신, 방어, 자원 재생, 원소 공격처럼 선택지가 많은 역할군은 새 일반 효과를 더 추가하지 않고 공개 테스트 피드백을 우선합니다.
- `v1.2.25`의 룬 파편 `4/3/2/1` 가중치, 기본 파편 드롭 판정 `8%`, 99회 무드롭 피티는 유지됩니다.

### Compatibility

- 세이브 및 어픽스·룬워드 직렬화 형식은 변경하지 않았습니다.
- 기존 어픽스 ID, 룬워드 ID, 생성 레코드 순서와 FormID 배치를 유지합니다.
- 기존 장비와 완성된 룬워드는 그대로 유지되며, 같은 ID의 개편된 효과 정의를 사용합니다.
- 무기 하위 유형 제한은 업데이트 후 새로 생성하거나 재련하는 장비에 적용되며 기존 장비를 강제로 재추첨하지 않습니다.
- 구버전 접미 패시브는 다음 런타임 동기화에서 현재 장비 상태에 맞게 자동 정리됩니다.

## [1.2.25] - 2026-07-12

`v1.2.25`는 공개 피드백에서 확인된 룬 파편 편중을 완화하고, 룬워드 성능 차이에 비해 과도했던 고급 룬 병목을 현실적인 범위로 줄이는 룬 경제 밸런스 프리릴리즈입니다.

### Added

- 룬 33종의 정확한 4단계 가중치, 4:1 상·하한 비율, SPID 94티켓 산출물과 런타임 계약을 고정하는 회귀 테스트를 추가했습니다.

### Changed

- 룬 가중치를 저급 `El-Amn=4`, 중급 `Sol-Um=3`, 고급 `Mal-Lo=2`, 최상급 `Sur-Zod=1`의 네 단계로 재조정했습니다.
- SKSE 상자/컨테이너 선택과 SPID 시체 레벨드 리스트가 같은 4:1 분포를 사용하도록 SSOT, 생성기 폴백과 DLL 폴백을 일치시켰습니다.
- 첫 10종 룬의 파편 내 점유율을 약 78.25%에서 42.55%로 낮추고, 최상급 룬 1종의 비중을 약 0.02~0.06%에서 약 1.064%로 조정했습니다.
- 누락 룬 폴백을 새 최상급 가중치 `1`로 낮춰 계약 누락이 오히려 최고 빈도로 역전되지 않도록 했습니다.

### Fixed

- Windows dotnet 래퍼를 사용하는 WSL 패키징에서 `/mnt/c` staging 경로가 잘못된 WSL UNC로 전달되어 Generator가 실패하던 문제를 Windows 경로 변환으로 수정했습니다.

### Balance

- 독립 가중 추첨·교환 없음 기준 94개 레시피의 완성 기대 파편 격차는 약 563배에서 8.64배로 줄어듭니다.
- 고급 룬워드는 크게 접근하기 쉬워지는 대신, 가장 쉬운 저급 레시피는 기존보다 약 2.55배 느려질 수 있습니다.
- 기본 파편 드롭 판정 `8%`와 99회 무드롭 피티는 변경하지 않았습니다.

### Compatibility

- 세이브·어픽스·룬워드 직렬화 형식은 변경하지 않았으며, 이미 보유한 룬 파편과 완성 룬워드는 그대로 유지됩니다.
- 새 분포는 업데이트 후 새로 선택되는 파편에 적용됩니다. 이미 SPID로 분배된 액터/시체 인벤토리는 즉시 다시 추첨되지 않습니다.

## [1.2.24] - 2026-07-11

`v1.2.24`는 무기 거치대가 사용하는 플레이어 인벤토리 → 월드 오브젝트 경로에서 어픽스 상태가 플레이어 UID에 남아, 이후 다른 창고에서 꺼낸 무기에 잘못 이어질 수 있던 문제를 막는 상태 소유권 핫픽스입니다.

### Added

- 드롭된 월드 참조 전용 상태 키, 동일 참조 재획득 검증, 핸들 재사용 차단, 중복 드롭 기록 갱신 및 기존 세이브의 orphan player-key 정리를 고정하는 회귀 테스트를 추가했습니다.

### Changed

- 플레이어가 드롭한 장비의 상태를 재사용 가능한 플레이어 UID 공간에서 제거하고, 실제 월드 참조 FormID에 임시 귀속합니다.
- 같은 월드 참조를 다시 주울 때에만 해당 상태를 새 플레이어 UID로 복원합니다.
- 월드 참조 삭제 대기열이 참조별 상태 키를 함께 보존하며, 세이브 로드·되돌리기 시 이전 세이브의 대기 항목을 초기화합니다.
- 모드를 다시 활성화할 때와 세이브를 로드할 때 실제 인벤토리에 없는 플레이어 소유 상태를 fail-closed 방식으로 정리합니다.

### Fixed

- 어픽스 무기 A를 무기 거치대에 건 뒤 다른 창고에서 꺼낸 무기 B가 A의 이전 UID를 재사용하면 A의 어픽스와 런타임 상태가 B에 보이던 문제를 수정했습니다.
- 저장·로드 뒤 드롭 추적 캐시가 초기화된 경우에도 동일 월드 참조를 통해 원래 장비 상태를 복원할 수 있도록 수정했습니다.
- 삭제된 월드 참조의 어픽스, 런타임 상태, 룬워드 상태, 프리뷰·평가 마커가 일부 남을 수 있던 정리 경로를 통합했습니다.
- 오래된 참조 핸들이 다른 월드 오브젝트에 재사용됐을 때 상태를 잘못 이전하거나 삭제할 수 있던 경로를 차단했습니다.

### Compatibility

- 세이브 및 어픽스 직렬화 레코드 버전은 변경하지 않았습니다.
- `v1.2.23`에서 무기를 거치대에 둔 채 아직 다른 아이템이 오염되지 않은 세이브는 로드 시 남은 플레이어 UID 상태를 안전하게 폐기합니다. 원본 거치대 무기의 상태는 증명할 방법이 없어 보존되지 않을 수 있습니다.
- 이미 다른 아이템 B에 A의 상태가 나타난 세이브는 자동으로 A/B를 구분할 수 없습니다. 해당 아이템에 `Reset State / 상태 초기화`를 사용하거나 문제가 발생하기 전 세이브에서 다시 시작해야 합니다.
- `v1.2.23`의 해상도별 툴팁 배율과 기존 설정 호환성은 그대로 유지됩니다.

## [1.2.23] - 2026-07-11

`v1.2.23`은 4K에서 확인된 기존 어픽스 툴팁 외관을 유지하면서 QHD, FHD, 울트라와이드 해상도에서 화면 비율에 맞게 크기와 기본 위치를 조정하는 UI 호환성 업데이트입니다.

### Added

- 4K, QHD, FHD, UWQHD의 툴팁 배율·기본 위치와 모바일 분기, 수동 글자 크기 저장 경로를 고정하는 회귀 테스트를 추가했습니다.

### Changed

- 툴팁 자동 배율 기준을 `3840x2160`의 기존 `2.5x` 외관으로 명시하고, 뷰포트의 가로·세로 중 작은 비율을 사용하도록 변경했습니다.
- 저장된 사용자 배치가 없을 때 툴팁 기본 `right/top` 위치도 같은 4K 기준으로 비례 조정합니다.

### Fixed

- QHD와 FHD에서 어픽스 툴팁이 4K보다 화면을 더 크게 차지하던 문제를 수정했습니다.
- `3440x1440` 같은 울트라와이드에서 가로 폭만 따라 툴팁이 과도하게 커질 수 있는 문제를 높이 기준 비율로 제한했습니다.

### Compatibility

- 세이브 및 어픽스 런타임 저장 형식은 변경하지 않았습니다.
- 기존 `prisma_tooltip_layout.json`의 수동 `right/top` 위치와 글자 크기는 그대로 유지됩니다. 현재 해상도의 새 기본 위치를 적용하려면 해당 해상도에서 툴팁 UI를 초기화해야 합니다.
- 폭 `900px` 이하 분기와 기존 글자 크기 조절 범위 `70%~180%`는 유지됩니다.

## [1.2.22] - 2026-07-10

`v1.2.22`는 아이템 판매·보관 중 ExtraUniqueID 소유권이 바뀔 때 인스턴스 상태가 끊기거나 다른 아이템으로 이어질 수 있던 문제를 막는 안전성 핫픽스입니다.

### Added

- 룬워드 패널에 선택 장비의 Calamity 어픽스, 룬워드 진행도, 런타임 상태, 프리뷰와 표시 이름을 제거하는 `Reset State / 상태 초기화` 동작을 추가했습니다.
- 실수 방지를 위해 상태 초기화는 6초 안에 두 번 눌러야 하며, 소모한 재련 오브와 룬 조각은 환불되지 않습니다.
- 판매·상자 보관·회수 및 목적지 키 충돌을 owner+UID 기준으로 검증하는 회귀 테스트를 추가했습니다.

### Changed

- `ExtraUniqueID` 인스턴스 키 의미를 SKSE 원본 계약인 `(owner FormID, UID)`로 명확히 통일했습니다.
- 구버전이 `(item FormID, UID)`로 만든 플레이어 인벤토리 키는 UID가 유일할 때만 로드 후 변환합니다. 같은 owner+UID에 애매한 기존 상태가 있으면 먼저 fail-closed로 제거해 현재 아이템에 잘못 붙지 않게 합니다.
- 생성물 최신성 검사는 파일 수정시각 대신 실제 JSON 콘텐츠를 비교하도록 변경했습니다.
- GitHub Actions가 fresh checkout에서도 xwin과 CommonLibSSE-NG를 외부 캐시에 준비한 뒤 실제 DLL/runtime-gate를 빌드하도록 보강했습니다.

### Fixed

- `TESUniqueIDChangeEvent`의 아이템 FormID를 플레이어 소유자 ID로 오인해 판매·상자 이동 이벤트를 놓치던 문제를 수정했습니다.
- UID 변경 목적지에 기존 상태가 있을 때 두 아이템의 어픽스 토큰을 합치던 동작을 제거했습니다. 소유자를 증명할 수 없는 충돌 상태는 양쪽 모두 fail-closed로 제거해 다른 아이템으로 이어지지 않게 합니다.
- 플레이어가 직접 생성한 `ExtraUniqueID`에 아이템 FormID를 기록하던 문제를 수정해 실제 소유자 FormID를 기록합니다.

### Compatibility

- 이미 다른 아이템에 잘못 붙어 버린 legacy 상태는 원래 소유자를 안전하게 추론할 수 없습니다. 해당 장비를 선택하고 `Reset State / 상태 초기화`를 사용해야 합니다.
- 상태 초기화 뒤 자동 어픽스 재롤을 막기 위해 evaluated 표시는 유지됩니다.

## [1.2.21] - 2026-03-22

`v1.2.21`은 `v1.2.20` 이후 확인된 룬워드 워크벤치 스크롤 UX 문제를 정리한 유지보수 릴리즈입니다.

### Changed

- Prisma 룬워드 레시피 목록이 전역 inertia 스크롤을 타지 않고, 목록 전용 amplified wheel scroll 경로를 사용하도록 조정했습니다.
- CEF가 작은 휠 델타를 줄 때도 의미 있게 내려가도록 최소 스크롤 step을 추가해, 휠을 과도하게 많이 굴려야 하던 문제를 완화했습니다.

### Fixed

- 룬워드 레시피 목록이 무겁게 끊기거나, 반대로 너무 조금씩만 내려가서 긴 목록 탐색이 불편하던 문제를 수정했습니다.

## [1.2.20] - 2026-03-22

상세한 RC 흐름은 [v1.2.20 RC 통합 노트](docs/releases/2026-03-22-v1.2.20-rc02-rc23-consolidated-notes.md)를 참고하세요.

### Added

- 룬워드 효과군을 스카이림 마법 학파 기반으로 재설계하고, 겹치던 효과를 고유 효과로 바꿔 빌드 선택지가 더 분명해졌습니다.
- Utility prefix 3종을 전투용 효과로 복귀시키고, suffix 패밀리를 재편해 장비 파밍 결과가 더 뚜렷하게 갈리도록 정리했습니다.
- Bloom 계열 트랩에 `planted / burst` 피드백과 생성 실패 진단을 추가해, 심김과 실제 발동을 인게임에서 바로 확인할 수 있게 했습니다.

### Changed

- 룬워드 워크벤치 UI를 재설계해 레시피 프리뷰 기본 오픈, 스크롤 복구, 레이아웃 밀도 조정, 패널 드래그/배치 개선을 한 사이클에 묶었습니다.
- 아이템 설명과 공개 문서를 대규모로 손봐, 한국어 설명이 더 구체적으로 보이고 내부용 정의는 플레이어 문서에서 숨기도록 정리했습니다.
- 트리거, 룬워드, 직렬화, Prisma 패널 관련 내부 구조를 분해해 이후 기능 수정과 검증이 쉬운 방향으로 정리했습니다.
- 특수 액션 계약을 강화해 잘못된 `runtime.trigger`나 암묵적인 `0 = always-on` 보정을 제거했고, 디버그 HUD와 verbose 로그도 분리했습니다.

### Fixed

- 플레이어 피격 시 `TESHitEvent` fallback 재진입으로 이어지던 전투 프리징을 수정하고, `HandleHealthDamage` hook 기반의 안정적인 incoming hit routing으로 되돌렸습니다.
- SE/AE 공용 CommonLibSSE 타깃이 깨져 Address Library 초기화가 실패하던 릴리즈 빌드 문제를 수정했습니다.
- `ConvertDamage`의 원소별 동시 적용, `AttackDamageMult`/`WeaponSpeedMult` magnitude 단위, 어픽스 개수 reroll, 방어구 suffix 드롭 풀 누락 같은 데이터/런타임 버그를 정리했습니다.
- 프로젝트 경로 이동 뒤 build tree가 깨지던 문제, generator/runtime contract sync 불안정, MO2 ZIP 패키징과 Papyrus compile 검증 경로를 보강해 배포 안정성을 높였습니다.

## [1.2.20-rc23] - 2026-03-09

### Added

- Bloom 계열 트랩에 `planted / burst` 진단 HUD와 생성 실패 사유 알림을 추가해, 트랩이 심어졌는지와 실제 발동했는지를 인게임에서 바로 확인할 수 있게 했습니다.

### Changed

- 특수 액션(`CastOnCrit`, `ConvertDamage`, `MindOverMatter`, `Archmage`, `CorpseExplosion`, `SummonCorpseExplosion`)은 이제 유효한 `runtime.trigger`와 명시적 `runtime.procChancePercent > 0`이 없으면 보정 없이 실패하도록 계약을 강화했습니다.
- 기존 특수 액션의 `procChancePercent: 0.0` 데이터는 모두 의미 보존 형태의 `100.0`으로 마이그레이션했습니다.
- 공개 문서 생성 경로에서 INTERNAL 항목을 기본 숨김 처리해, 플레이어용 카탈로그와 prefix 문서가 내부 companion spell 정의를 직접 드러내지 않도록 정리했습니다.
- `debug notifications` 설정을 `debugHudNotifications`와 `debugVerboseLogging`으로 분리하고, legacy `debugNotifications`는 호환용 입력으로만 읽도록 바꿨습니다.

### Fixed

- debug HUD/log 분리 과정에서 룬워드 안내, 복구 알림, 설정 변경 피드백 같은 일반 HUD 메시지까지 숨겨지던 회귀를 수정했습니다.
- release/build 검증 스크립트가 `dotnet` 실행 경로 문제로 테스트 커버리지를 잃지 않도록 `CAFF_DOTNET` 주입 경로와 fake-`dotnet` 기반 테스트를 추가했습니다.

## [1.2.20-rc22] - 2026-03-08

### Fixed

- 플레이어 피격 시 `TESHitEvent` fallback incoming trigger가 proc 후속 이벤트를 다시 먹으며 프리징으로 이어질 수 있던 경로를 재진입 guard로 차단했습니다.
- `PlayerCharacter HandleHealthDamage` hook를 기본 경로로 사용하도록 바꿔, 플레이어 incoming hit 처리가 불안정한 fallback 경로에 과도하게 의존하던 문제를 줄였습니다.
- 예전 `allowPlayerHealthDamageHook` 런타임 override는 무시하고 항상 플레이어 피격 hook를 활성화하도록 정리했습니다.

## [1.2.20-rc21] - 2026-03-08

### Fixed

- 릴리즈 빌드가 `CommonLibSSE-NG`의 `install.linux-clangcl-rel-se`를 참조하던 문제를 `install.linux-clangcl-rel` flatrim(SE/AE 공용) 기준으로 수정했습니다.
- `ensure_skse_build.py`가 `CMAKE_PREFIX_PATH`까지 검사해, 잘못된 SE 캐시를 유지한 채 패키징하는 상황을 막도록 보강했습니다.

## [1.2.20-rc20] - 2026-03-08

### Changed

- Prisma control panel 드래그 중 위치 갱신을 `requestAnimationFrame + transform` 기반으로 정리해 레이아웃 흔들림과 잦은 style write를 줄였습니다.

### Fixed

- 대형 Prisma 패널을 끌 때 드래그가 무겁거나 미세하게 튀는 체감 문제를 완화했습니다.
- 패널 드래그/리사이즈/복원 경로가 서로 다른 `left/top/right/bottom` 갱신 방식을 섞던 부분을 공통 anchored-position helper로 맞췄습니다.

## [1.2.20-rc19] - 2026-03-08

### Added

- 프로젝트 경로 이동 뒤에도 `ensure_skse_build.py`와 no-space run-root helper가 stale build tree를 자동 복구하도록 정리했습니다.
- `HealthDamage` guard 조합을 fake 이벤트 시퀀스로 직접 검증하는 runtime-gate 테스트를 추가했습니다.
- `Transmute` fragment consume/rollback helper와 `Reforge` regular-slot reroll helper를 분리해 동작 기반 검증 지점을 늘렸습니다.

### Changed

- SKSE 플러그인 버전 메타데이터가 CMake 생성 버전을 단일 진실원으로 사용하도록 정리했습니다.
- `EventBridge`의 serialization load, trigger runtime, runeword selection/transmute/reforge 흐름을 helper 중심으로 더 잘게 분해했습니다.
- `PrismaTooltip` 명령/상태 처리를 controller와 view-state helper 기준으로 재배치했습니다.

### Fixed

- 프로젝트 경로 변경 뒤 `ctest`/release verification이 예전 절대경로에 묶여 깨지던 문제를 줄였습니다.
- `HealthDamage` stale-window, per-target repeat, low-health snapshot 검증이 문자열 검색에 과도하게 의존하던 상태를 보강했습니다.
- 룬워드 재련 시 보존해야 하는 runeword token과 regular affix reroll 비교 경로를 분리해 회귀 위험을 낮췄습니다.

[Unreleased]: https://github.com/servaltullius/calamity-reactive-loot-affixes/compare/v1.4.0...HEAD
[1.4.0]: https://github.com/servaltullius/calamity-reactive-loot-affixes/compare/v1.3.3...v1.4.0
[1.3.3]: https://github.com/servaltullius/calamity-reactive-loot-affixes/compare/v1.3.2...v1.3.3
[1.3.2]: https://github.com/servaltullius/calamity-reactive-loot-affixes/compare/v1.3.1...v1.3.2
[1.3.1]: https://github.com/servaltullius/calamity-reactive-loot-affixes/compare/v1.3.0...v1.3.1
[1.3.0]: https://github.com/servaltullius/calamity-reactive-loot-affixes/compare/v1.2.25...v1.3.0
[1.2.25]: https://github.com/servaltullius/calamity-reactive-loot-affixes/compare/v1.2.24...v1.2.25
[1.2.24]: https://github.com/servaltullius/calamity-reactive-loot-affixes/compare/v1.2.23...v1.2.24
[1.2.23]: https://github.com/servaltullius/calamity-reactive-loot-affixes/compare/v1.2.22...v1.2.23
[1.2.22]: https://github.com/servaltullius/calamity-reactive-loot-affixes/compare/v1.2.21...v1.2.22
[1.2.21]: https://github.com/servaltullius/calamity-reactive-loot-affixes/compare/v1.2.20...v1.2.21
[1.2.20]: https://github.com/servaltullius/calamity-reactive-loot-affixes/compare/v1.2.19.2...v1.2.20
[1.2.20-rc23]: https://github.com/servaltullius/calamity-reactive-loot-affixes/compare/v1.2.20-rc22...v1.2.20-rc23
[1.2.20-rc22]: https://github.com/servaltullius/calamity-reactive-loot-affixes/compare/v1.2.20-rc21...v1.2.20-rc22
[1.2.20-rc21]: https://github.com/servaltullius/calamity-reactive-loot-affixes/compare/v1.2.20-rc20...v1.2.20-rc21
[1.2.20-rc20]: https://github.com/servaltullius/calamity-reactive-loot-affixes/compare/v1.2.20-rc19...v1.2.20-rc20
[1.2.20-rc19]: https://github.com/servaltullius/calamity-reactive-loot-affixes/compare/v1.2.20-rc18...v1.2.20-rc19
